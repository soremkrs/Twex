import React from "react";
import { Box, Typography } from "@mui/material";

function MainFeed() {
  return (
    <Box flex={1} p={2} maxWidth="600px">
      <Typography variant="h6">Main Feed</Typography>
      {/* Map over posts here later */}
    </Box>
  );
}

export default MainFeed;
